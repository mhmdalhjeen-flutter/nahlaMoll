# Nahla Mall — Brand Assets

Place the approved logo files here (do not commit placeholder artwork):

| File | Usage |
|------|--------|
| `nahla-mall-logo.svg` | Desktop header, sidebar, auth screens |
| `nahla-mall-logo.png` | Raster fallback for full logo |
| `nahla-mall-icon.svg` | Mobile header, compact UI |
| `nahla-mall-icon.png` | Icon raster fallback |
| `favicon.png` | Browser tab / PWA (recommended 1024×1024 source) |

Referenced from `src/lib/branding.ts` → `BRAND_ASSETS`.

Until files are added, `BrandLockup` falls back to the Arabic wordmark.
